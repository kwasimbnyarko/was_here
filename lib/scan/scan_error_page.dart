import 'package:flutter/cupertino.dart';

class ScanErrorPage extends StatelessWidget {
  const ScanErrorPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
